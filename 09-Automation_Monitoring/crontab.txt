# Student Performance Analysis - nightly batch run
# Runs every night at 02:00 server time, appends stdout/stderr to a log file.
0 2 * * * cd /opt/student-performance/09-Automation_Monitoring && /usr/bin/python3 nightly_job.py >> logs/cron_stdout.log 2>&1
